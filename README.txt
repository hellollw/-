训练过程：
 步骤		存档
特征提取		特征值，每幅图片的特征数量，标签值
生成词典		保存Kmeans训练集（之后可以使用该类中的cluster_centers调出簇中心等数据）
编码		训练样本的降维编码,整体标签名称（确定标签的一致性）
分类器		分类器参数

生成器：(类似于一个可迭代对象，但其可以实时生成数据）——yield关键符！！
    可迭代
    只能读取一次
    实时生成数据，不全存在内存当中
	每次的运行到yield返回yield后面的值，下一次直接从yield下一行开始运行